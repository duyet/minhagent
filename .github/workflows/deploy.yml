name: Deploy to Cloudflare Workers

on:
  push:
    branches: [main]
    paths:
      - "apps/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/**"

concurrency:
  group: deploy-cloudflare
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build web
        run: cd apps/web && pnpm run build

      - name: Typecheck
        run: pnpm run typecheck

      - name: Deploy webhook
        run: cd apps/webhook && npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy web
        run: |
          cd apps/web
          # Add main field back for wrangler deploy (removed from config to avoid
          # vite-plugin validation during build; the file now exists post-build)
          sed -i 's/"nodejs_compat"/"nodejs_compat",\n  "main": ".\/dist\/_worker.js\/index.js"/' wrangler.jsonc
          npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
